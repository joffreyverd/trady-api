// import { Position } from '../types';

// const getByUserId = (db: any, id: string) => {
//   const { getByUserIdFn } = this!;
//   if (!getByUserIdFn) {
//     throw new Error('(getByUserIdFn) not implemented');
//   }
//   return this.getByUserIdFn(db, id);
// };

// const create = (db: any, position: Position) => {
//   const { createFn } = this!;
//   if (!createFn) {
//     throw new Error('(createFn) not implemented');
//   }
//   return this.createFn(db, position);
// };

// export default { getByUserId, create };
